package algorithms

fun GaussMethod(a: List<List<Double>>, b: List<Double>) {
    if (a[0].size != b.size) return
    for (i in a.indices) {
        for (j in 0 until a[0].size) {
            if (a[i][j] != 0.0) {

            }
        }
    }
}